⚡ ESNext Features in React Projects
---------------------

[9] Optional Chaining & Nullish Coalescing
  - Create a nested object user?.profile?.avatar ?? "default.png".
  - Use it in a React component that gracefully handles missing props.

[10] Dynamic Imports
  - Create a React component that lazy-loads a chart library only when needed.
  - Bonus: Add React Suspense + fallback UI.

[11] Destructuring + Rest/Spread
  - Write a custom React hook that returns some config.
  - Use destructuring with rest props so consumers can extend or override defaults.

[12] Generators + Async Iterators
  - Build a function that yields mock API results page by page (like infinite scroll).
  - In React, hook it up to a “Load More” button.